---
import "../styles/lumen.css";
import HeaderSettings from "../components/HeaderSettings.tsx";
import ChatSidebar from "../components/ChatSidebar.tsx";
import TourMenuButton from "../components/TourMenuButton.tsx";
import SourcecodeMenuButton from "../components/SourcecodeMenuButton.tsx";
import SourcecodeOverlayHost from "../components/SourcecodeOverlayHost.tsx";
import GpuBufferWarningBanner from "../components/GpuBufferWarningBanner.tsx";

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "Gemma Data Agent",
  description = "Gemma Data Agent — data analysis agents that run entirely in your browser",
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@200;300;400;500;600;700&family=IBM+Plex+Mono:wght@300;400;500&family=IBM+Plex+Serif:ital,wght@0,300;0,400;1,300&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,300..500,0..1,-25..0&display=block"
      rel="stylesheet"
    />
  </head>
  <body class="has-chat">
    <GpuBufferWarningBanner client:only="react" />
    <nav class="nav">
      <div class="nav-inner">
        <a href="/" class="brand">
          <h1><em>Gemma Data Agent</em></h1>
        </a>
        <div class="nav-right">
          <div class="nav-links">
            <SourcecodeMenuButton client:only="react" />
            <TourMenuButton client:only="react" />
            <a href="https://github.com/nlothian/gemma-data-agents/">GitHub</a>
          </div>
          <HeaderSettings client:load />
        </div>
      </div>
    </nav>
    <main class="container">
      <slot />
    </main>
    <ChatSidebar client:only="react" />
    <SourcecodeOverlayHost client:only="react" />
  </body>
</html>
